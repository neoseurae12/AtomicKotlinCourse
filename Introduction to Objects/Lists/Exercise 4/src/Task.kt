// Lists/Task4.kt
package listsExercise4

// Parameter Info => ⌘ P
  // This action is convenient when you are typing arguments and want to check which argument is expected at the current position.

fun main() {
  val list = listOf('a', 'b', 'c', 'd', 'e').subList(1, 3)
  println(list)
}